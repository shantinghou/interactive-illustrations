{
    "tab": "Hover",
    "feature": "Glow on Hover",
    "featureDescription": "The GlowOnHover component applies a soft glowing effect to elements within an SVG when hovered. Only elements with the class 'glow' will respond.",
    "params": [
        {
        "prop": "src",
        "description": "(type: React.ReactElement), Required — the SVG element or group to apply glow behavior to"
        },
        {
        "prop": "width",
        "description": "(type: number), Width of the container SVG (default: 300)"
        },
        {
        "prop": "height",
        "description": "(type: number), Height of the container SVG (default: 300)"
        },
        {
        "prop": "scaleFactor",
        "description": "(type: number), Scale multiplier applied on hover (default: 1.2)"
        },
        {
        "prop": "color",
        "description": "(type: string), Glow color applied as drop-shadow (default: '#D9D9D9')"
        }
    ],
    "svg-requirement": {
        "explanation": "Only SVG elements with the class name 'glow' will respond to hover with the glowing effect.",
        "classes": [
        {
            "name": "glow",
            "description": "Marks the SVG elements that will glow and scale when hovered."
        }
        ],
        "example": "<svg width=\"100\" height=\"100\" xmlns=\"http://www.w3.org/2000/svg\">\n  <circle class=\"glow\" cx=\"50\" cy=\"50\" r=\"30\" fill=\"gold\" />\n</svg>"
    },
    "examples": [
        {"title": "Glow effect on specific element inside SVG", "file": "ExampleGlowOnHover", "code": "<GlowOnHover src={<Example/>} width={200} color={\"#F57F6D\"}/>"}
    ]
  }